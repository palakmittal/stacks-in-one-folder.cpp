#include<iostream>
#include<stack>
#include<string.h>
using namespace std;

int main(){

string str="hellojee";
char ch;
stack<char>st;

for(int i=0;i<str.size();i++){
  ch=str[i];
  st.push(ch);
}

while(!st.empty()){
  cout<<st.top()<<"";
 st.pop();
}
  return 0;
}
